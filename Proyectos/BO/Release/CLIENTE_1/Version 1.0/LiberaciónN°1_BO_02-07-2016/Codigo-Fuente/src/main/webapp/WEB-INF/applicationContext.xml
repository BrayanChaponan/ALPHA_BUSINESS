<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
        http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.0.xsd">

	<!-- Cargar Properties al contexto Spring -->
	<context:property-placeholder location="classpath:properties/BOAlpha.properties" />

	<!-- Paquete Base para escanear anotaciones Spring -->
	<context:component-scan base-package="com.alphabuss.backoffice" />

	<!-- Configuracion de conexionex de BDs -->
	<bean id="DataSource" class="org.apache.commons.dbcp.BasicDataSource"
		destroy-method="close">
		<property name="driverClassName" value="${jdbc.driverClass}" />
		<property name="url" value="${jdbc.url}" />
		<property name="username" value="${jdbc.user}" />
		<property name="password" value="${jdbc.password}" />
	</bean>

	<!-- MyBatis - Fabrica de Sesiones -->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">

		<!-- Referencia a una conexion de base datos -->
		<property name="dataSource" ref="DataSource" />

		<!-- Ubicacion de los Mappers XML -->
		<property name="mapperLocations" value="classpath:mybatis/mappers/*.xml" />

		<!-- Alias de clases de Modelo (Se evita especificar alias por Modelo en 
			el mybatis-config.xml) -->
		<property name="typeAliasesPackage"
			value="com.alphabuss.backoffice.model, com.alphabuss.backoffice.bean" />

		<!-- Ubicacion de mybatis-config.xml -->
		<property name="configLocation" value="classpath:mybatis/config/mybatis-config.xml"></property>
	</bean>

	<!-- MyBatis Escaner de Interfaces Mappers -->
	<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
		<property name="basePackage" value="com.alphabuss.backoffice.dao" />
		<property name="sqlSessionFactoryBeanName" value="sqlSessionFactory" />
	</bean>

	<!-- Gestor de transacciones basado en anotaciones (Ejm: @Transactional) -->
	<bean id="txnManager"
		class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="DataSource" />
	</bean>

	<!-- Habilitar Gestor de transacciones -->
	<tx:annotation-driven transaction-manager="txnManager" />

</beans>
